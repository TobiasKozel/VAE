#ifndef VAE_ENGINE
#define VAE_ENGINE

#include <cstddef>

namespace vae { namespace core {
	class Engine {
		size_t mTime = 0; // Global time in samples
	};
} } // namespace vae::core

#endif // VAE_ENGINE
